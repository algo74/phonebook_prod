# Backend of Phonebook app

The code for deployment at renderer.com is build in a git submodule at `../phonebook_prod`
by running a script in this directory

The app is live at https://phonebook-prod-onpb.onrender.com/

The frontend is in `../phonebook`